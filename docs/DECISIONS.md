# DECISIONS

> 프로젝트 결정 기록. 형식: 날짜 · 결정 · 이유 · 영향. 뒤집힌 결정은 지우지 말고 ~~취소선~~ + 후속 결정 링크.

| # | 날짜 | 결정 | 이유 | 영향 |
|:--:|:--:|---|---|---|
| D-001 | 7/1 | 데이터 계층(수집·트리거·필터·RCA)은 FastAPI 집중, Spring은 저장·조회만 | 트리거를 Spring에 두면 원천 소비 wrapper 중복 | 아키텍처 경계 확정 (NFR-04) |
| D-002 | 7/1 | ~~POC = 실시간 view (트리거 감지~분석과정 front 표시)~~ → D-018로 MVP 제외 | 발표 데모 가치 | ~~D7 MVP 정의서·D4 화면 설계에 포함 필수~~ |
| D-003 | 7/1 | 트리거 3계층: metric>cpu usage · trace>error string · log>start log 2회 | 3개 시나리오가 서로 다른 모달 신호로 잡힘 | D5 명세의 기준 |
| D-004 | 7/3 | 기획서·요구사항 정의서를 로컬 `README.md` 한 파일로 통합 | 두 문서 내용 중복 多 — 분리 시 어긋남 위험. 정본은 Notion | 세부 갱신은 Notion, 착수 관리는 README |
| D-005 | 7/3 | 슬라이스는 수직(시나리오 관통) 분해, S1 = walking skeleton (`cpu_contention` 얇게 E2E) | 통합 리스크를 초반(7/17)에 노출. WBS 수평 분해와 상호보완 | 슬라이스-분해표 구조 |
| D-006 | 7/3 | 슬라이스 표의 일정·담당은 WBS(01. 진행현황) 기준으로 정합, WBS ID 매핑 유지 | WBS가 팀 공식 일정 | WBS 변경 시 슬라이스 표 동기화 필요 |
| D-007 | 7/3 | WBS의 D7(MVP 정의서)·D8(슬라이스) 담당 오류 수정 → 이석진 | 배분 브리핑이 정본 | WBS 수정 완료 (본인) |
| D-008 | 7/3 | S1 내부 관통 목표 = 7/17 (WBS I4·I6 마감 7/20보다 선행) | E2E 첫 확인이 M1 당일(7/20)이면 도박 — 최소버전으로 먼저 꿰기 | I4·I5·I6 담당자와 "최소버전 우선" 합의 필요 |
| D-009 | 7/3 | RCA 채점 미통과 시 스냅샷 번들 정제(Top-K·요약) 튜닝을 프롬프트 수정보다 우선 | 입력 품질 > 프롬프트 (garbage in, garbage out) | S6 실패 대응 순서 |
| D-010 | 7/3 | 정규화 스키마: 조인 키 `ts`(UTC epoch ms)+`service`(canonical)를 공유하는 3개 플랫 테이블(logs·metrics·spans), Parquet 저장 | 모달리티별 페이로드 상이 — 단일 테이블 무리. Parquet은 ±Δ 시간 슬라이스가 빠름(detect-then-fetch) | FR-D-02. 정본: `docs/normalization-schema.md` + Notion 내부용 문서 |
| D-011 | 7/3 | canonical 서비스명 = trace serviceName 체계(kebab-case, 12종). 예외 매핑: 로그 `NginxThrift`·메트릭 `nginx-thrift` → `nginx-web-server` | 세 모달리티 표기 불일치 실측 (CamelCase/kebab 혼재). trace 체계가 전 서비스 커버 | 정규화·조인·원인 서비스 표기 기준 |
| D-012 | 7/3 | ingest 파일 화이트리스트: drop 3 — `NginxThrift_.log`(항상 0B)·`metadata.txt`(정보 無)·`all_traces.json`(csv와 중복, 4.5배 큼) + 보류 1 — `jaeger_spans_rate.csv`(수집 인프라 지표, baseline만). `summary.txt`는 참고용(ingest 제외) | 파일별 컬럼 실측 — 나머지는 전부 유효 컬럼 보유. req_id 두 체계 실측(log 내부 ID ≠ trace wrk2 ID)으로 log↔trace 조인은 D-010의 ts+service 방식 재확인 | ingest 대상 확정. drop 파일도 normal 파일목록(부재 감지 기준)에는 유지. 정본: Notion 내부용 문서 "AnoMod-data 파일 레벨 분석" |
| D-013 | 7/3 | MVP 권한: 인증 없음(비로그인 조회 허용) — 내부 팀 도구 한정 | 조회자 단일 액터, MVP 범위 최소화 | MVP-정의서 §7.4. 외부 공개 시 재검토 |
| D-014 | 7/7 | MVP 범위 기준 = 노션 기능정의서 v0.1 기능ID(EDGE/API/SVC/UI/VAL) + 유스케이스 v0.1 UC·MoSCoW 체계. 우선순위 매핑 **M=P0 · S=P1(검증은 Gate3 필수) · C=P2 이후**. 정본은 MVP-정의서 v0.2 부록 추적표 | 우선순위 3체계(M/S/C·P0~P2·포함/제외) 혼재 해소, 엣지 SDK 아키텍처(UC-19·/ingest) 반영 | MVP-정의서 v0.2·슬라이스 분해표 v0.3 재구성. ~~POC 실시간 view는 API-07·UI-03·UC-21 로컬 신설~~ → D-015 |
| D-015 | 7/7 | 기능정의서·유스케이스에 없는 항목은 기준 문서에서 ID를 만들지 않음 — 로컬 신설 ID(API-07·UI-03·UC-21) 철회. POC 실시간 view(D-002)는 MVP-정의서 §6 **서술로만** 범위 정의, 노션 반영 여부는 팀 논의(기초 MVP 후 후순위) | 기준 문서가 정본(노션)에 없는 ID를 발명하면 이중 정의·혼선 발생 | MVP-정의서 §4·§6·§10·부록, 슬라이스 S5 표기 수정. ID 부여는 노션 문서 담당 몫 |
| D-016 | 7/7 | **MVP 성공 = E2E 동작 + 리포트 프론트 조회** (기획서 §7 기준). 정확도 검증(3/3)은 성공 기준에서 분리 — 별도 검증 단계(Gate3 채점, VAL-01) 지표 | 7/7 회의: 성공 기준에 검증 포함 시 기획서와 정합 이슈. 기존 "부분 성공" 조건이 실제 성공 기준 | MVP-정의서 v0.3 §1·2·5·8, 슬라이스 v0.4 DoD S1·S3·S4(정확도→S6) 수정. VAL-01=S 모순 해소. 유스케이스 "맞히면 통과" 수정은 예지 |
| D-017 | 7/7 | I7(RCA 종합)·I8(에이전트↔Spring 연동)을 7/20(M1) 전으로 당김. I8은 Spring 선행 시 mock 연동 확인 후 교체. M1 RCA는 멀티 최소버전(모달3+종합) 기준 — agent 구조(단일 vs 3종+종합)는 예지·가희 ~7/9 결정 | M1에 리포트 프론트 조회가 성공 조건인데 I7·I8이 차주면 불가능 (일정 구멍) | WBS §3.4 수정(연지 완료), 슬라이스 S2 일정 표기 수정. 구조 결정 시 1.4·S2 재구성 |
| D-018 | 7/7 | **실시간 분석 과정 표시(실시간 view) MVP 제외** — D-002 뒤집음. 확장 백로그로 이동 (발표 데모 가치 재평가 시 재논의) | 기획서 v0.2·요구사항(FR)·기능정의서·유스케이스 어디에도 근거 없음(유일 근거 = 7/1 회의). D-016으로 MVP 성공 기준이 "조회까지" 확정되며 필수성 소멸 | MVP-정의서 §6 제외 명시·범위표·스토리·UI표·추적표에서 제거, 슬라이스 **S5 삭제**. ⚠️ 팀 공유 필요 + 화면 설계(D4, 혜림)에 실시간 view 있으면 동기화 |
| D-019 | 7/7 | **저장 주체 = Spring (A안)** — FastAPI는 DB 직접 접근 금지, `POST/PATCH /api/internal/reports*` 호출. DDL·엔티티 소유 Spring 단독 (Flyway V1 + ddl-auto=validate). 인덱스 기준: 실쿼리 경로만 — 자식 `(report_id, ts)` 복합, report `(status, created_at)`, 멱등은 `bundle_id` UNIQUE | api-spec 초안(B안: FastAPI 직접 DB)이 SVC-01·D-001·I8과 충돌 → 정본대로 확정. 두 앱의 스키마 공유 결합 제거 | api-spec §0·§5, schema.sql·schema.md 반영. I8 = internal API 2종. 7/8 회의 공유 |
| D-020 | 7/9 | **기준 정본 = 기능정의서·유스케이스 v0.2로 승격** (D-014 개정) — ① VAL-01·02(UC-16·17) 우선순위 S→C 하향, 매핑 규칙에 예외 명시(VAL-01은 C지만 Gate3 채점 필수, D-016 유지) ② VAL-02 재정의: LOSO → "정답 비공개 원인·유형 맞히기" ③ LLM 입력 정제 원칙: 시나리오 명칭·정답 라벨·파일명 제거 — `/ingest`의 title·bundle_id는 LLM 입력 제외 ④ SVC-01 "사용 raw" → "스냅샷 번들" 명칭 통일, 액터 "분석 시스템" 폐기(엣지/백엔드 분리) + QA 신설 | v0.2가 7/7 회의(D-016) 반영본 — v0.1 기준(D-014)이 낡음 | MVP-정의서 v0.3.2·슬라이스 v0.4(7/9②)·api-spec §3.1 반영. ⚠️ 유스케이스 v0.2 "MVP 경계"에 전송(/ingest) 단계 누락 — 예지에게 추가 요청 |

## 미결 (Open Questions)

| # | 질문 | 배경 | 기한 |
|:--:|---|---|:--:|
| Q-001 | D5 트리거 담당: 박가희·고유경(브리핑) vs 고유경·이예지(WBS)? | 두 문서 충돌 | 7/7 |
| Q-002 | D6 스냅샷번들에 이예지 포함? (브리핑 4인 vs WBS 3인) | 두 문서 충돌 | 7/7 |
| Q-003 | 시나리오별 기대 원인 서비스 이름 (`cpu_contention` 등) | 데이터 라벨 확인 필요 → MVP 검수표 확정 | 7/11 |
| Q-004 | 스냅샷 번들 ±Δ 시간창·Top-K 초기값 | 설계 단계 상세화 항목 (D6) | 7/9 |
| Q-005 | D-003의 "trace>error string" 신호 재정의 — svc_kill 실데이터에 error tag 0건·status 전부 200 | kill(00:01:57)→재시작(00:03:41)이 trace 수집 창(00:04:49~) 이전 종료. 실신호 = 로그 침묵 104초 + start log 2회 (스키마 설계서 §6 참조). **7/7 고유경 Perf 분석 결함군 대비(Svc_Kill=다운타임형·로그 재시작 마커)와 일치 — 공식 확정만 남음** | 7/9 |
| Q-006 | agent 구조: 단일 vs 3종모달+종합? | 7/7 회의 — 아키텍처 구성도는 3종+종합으로 작성돼 있으나 논의 중. 단일로 갈 경우 기획·일정·연관 문서 재구성. 결정: 예지·가희 | 7/9 |
